$(document).tooltip({
    trigger: 'hover',
    selector: '[rel*="tooltip"]',
    container: 'body'
});

$(document).popover({
    trigger: 'hover',
    selector: '[rel*="popover"]',
    container: 'body'
});